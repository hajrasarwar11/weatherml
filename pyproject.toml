[project]
name = "weatherml"
version = "1.0.0"
description = "AI Weather Analytics Platform — Random Forest classifier for weather prediction"
requires-python = ">=3.11"
dependencies = [
    "joblib>=1.5.3",
    "numpy>=2.4.3",
    "pandas>=3.0.1",
    "scikit-learn>=1.8.0",
]
